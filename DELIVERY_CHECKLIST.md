# Agent2 交付验收清单（v1.0）

> 用途：交付给客户前的逐项验收。每一项需确认"通过 / 待办 / 不适用"。
> 项目：资产自动发现 + 漏洞验证 Agent（Agent2）
> 生成日期：2026-09-06

---

## 一、交付物清单

| # | 交付物 | 路径 | 状态 |
| --- | --- | --- | --- |
| 1 | 主程序（CLI 入口） | `main.py` | ✅ |
| 2 | 核心包（发现/验证/报告/导出/Web/告警） | `asset_agent/` | ✅ |
| 3 | Web 仪表盘前端 | `webui/index.html` | ✅ |
| 4 | 使用文档 | `README.md` | ✅ |
| 5 | 授权范围模板 | `scope.example.json` | ✅ |
| 6 | 单元测试 | `tests/test_agent2.py` | ✅ |
| 7 | 演示靶机（可选，测试用） | `lab/vuln_target.py` | ✅ |

## 二、功能验收项

| # | 能力 | 验收命令 | 结果 |
| --- | --- | --- | --- |
| 1 | 授权范围强制（域名/IP/CIDR，越界拒绝） | `scope init/show` | ✅ 实测 |
| 2 | 子域名被动发现（DNS + crt.sh） | `discover subdomains` | ✅ 实测 |
| 3 | 端口扫描（TCP connect 多线程） | `discover ports` | ✅ 实测 |
| 4 | 服务指纹 + 技术栈识别 | `discover fingerprint` | ✅ 实测 |
| 5 | 漏洞库匹配（CPE 版本级，exact/product） | `verify match` | ✅ 真实库 3868 条命中 |
| 6 | 主动漏洞检测（18 类，只读非破坏） | `verify explore` | ✅ 实测 18 项 |
| 7 | 安全检查（响应头/暴露面/TRACE/证书） | `verify checks` | ✅ 实测 |
| 8 | 风险评分（0-10，与 agent1 同口径） | `assets list` | ✅ 实测 |
| 9 | HTML/Markdown 报告 | `report` | ✅ 实测 |
| 10 | Web 仪表盘（5 视图） | `web` | ✅ 实测 API |
| 11 | 导出 CSV/JSON/MD | `export` | ✅ 实测 |
| 12 | 快照与差异对比 | `snapshot diff` | ✅ 实测 |
| 13 | 告警推送（飞书/Webhook） | `notify push` | ⚠️ 需客户提供 Webhook 实测 |
| 14 | 一键自动扫描 | `scan --target` | ✅ 端到端实测 |

## 三、测试与验证结果

| 项 | 结果 |
| --- | --- |
| 单元测试 | **66/66 通过**（`python -m unittest discover -s tests`） |
| 端到端冒烟 | 本地靶机全流程通过（发现→匹配→主动检测→报告） |
| 漏洞库匹配 | 真实 CVE 命中验证（CVE-2012-1823 等，含 KEV 标记） |
| 越界拒绝 | 未授权目标被正确拒绝 |
| 依赖 | 纯 Python 标准库，零第三方依赖，Python 3.10+ |

## 四、部署要求（客户环境）

| # | 要求 | 说明 |
| --- | --- | --- |
| 1 | Python 3.10+ | 无需安装任何第三方包 |
| 2 | agent1 漏洞库 | 依赖 `../data/vulns.db`（3868 条 CVE，NVD/KEV 等），需随交付提供或用 `--vuln-db` 指定 |
| 3 | 授权范围配置 | 首次运行前编辑 `data/scope.json` 填入客户授权资产 |
| 4 | 网络访问 | crt.sh 证书日志需外网；内网资产扫描仅需内网可达 |
| 5 | 操作系统 | Windows / Linux / macOS 均可（命令行执行） |

## 五、客户侧需提供/确认（交付前必办）

- [ ] **授权范围清单**：客户待扫描的域名 / IP / CIDR（写入 `data/scope.json`，这是法律红线）
- [ ] **书面授权确认**：建议在合同/委托书中明确"仅授权扫描上述资产"，我方仅承担授权范围内评估
- [ ] **漏洞库路径**：确认 agent1 `vulns.db` 随交付提供，或告知自定义路径
- [ ] **告警通道**（可选）：飞书机器人 Webhook 地址，用于 `notify push`
- [ ] **验收数据**：客户提供 1-2 个测试资产，跑通 `scan` 全流程并核对报告

## 六、已知限制与边界（须向客户说明）

1. **检测 = 证据链验证，非利用确认**：`exact` 表示版本证据闭合，不代表漏洞可利用；`product` 级需人工确认版本。
2. **主动检测全部只读非破坏**：无攻击载荷、无爆破、无写入，不承担渗透测试深度（如利用链验证、绕过测试）。
3. **漏洞库时效**：漏洞覆盖面取决于随交付的 `vulns.db` 同步时间，需定期用 agent1 同步。
4. **覆盖范围**：子域名发现依赖 DNS/crt.sh 被动源；端口扫描默认端口列表可扩展。
5. **网络环境影响**：目标网络防火墙、WAF 可能影响检测结果，建议在目标网络内执行。

## 七、合规声明（建议随交付附上）

- 本工具仅限对**自己拥有或已获书面授权**的资产使用。
- 未经授权对他人系统进行扫描探测，在中国大陆可能违反《网络安全法》《刑法》第 285 条，交付方与使用方均需承担相应法律风险。
- 建议在客户合同、项目启动文档中写入授权范围与使用边界。

---

## 结论

**功能与质量层面：已具备 v1.0 交付条件（✅ 66 项测试通过、端到端实测）。**

**交付动作层面：还差 3 件事，均需客户侧配合：**
1. 客户提供授权范围清单 → 写入 `scope.json`
2. 确认漏洞库随交付提供 → 部署时验证 `verify match` 可读
3. （可选）客户提供 Webhook → 实测 `notify push`

完成上述 3 项后即可正式交付。
