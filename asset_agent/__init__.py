"""AssetAgent2 —— 资产自动发现 + 漏洞验证 Agent。

定位：对**已授权**的资产做被动/轻量探测，自动建立资产清单，
并通过指纹 + CPE 版本范围与本地漏洞库（agent1 的 vulns.db）做
**非破坏性、证据链式**的漏洞匹配验证。

设计红线：
  1. 所有发现/验证动作必须先通过 Scope（授权范围）检查，越界目标直接拒绝；
  2. 验证只做指纹采集、版本比对与只读 GET 探测，不包含任何利用/攻击载荷；
  3. 只用于扫描自己拥有或已获书面授权的资产。
"""

__version__ = "0.1.0"
